# Tests use Testcontainers, not local Docker Compose
spring.docker.compose.enabled=false

# JWT (CI may export empty JWT_SECRET; tests must not depend on external env)
app.security.jwt.secret=test-jwt-secret-for-ci-and-local-tests-32chars
